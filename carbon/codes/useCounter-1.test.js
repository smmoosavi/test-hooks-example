// useCounter.test.js

import React from 'react';
import { mount } from 'enzyme';

const fn = jest.fn();

describe('useCounter hook', () => {
  beforeEach(() => fn.mockClear());

  // TODO: add tests
});
